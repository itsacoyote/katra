# better-sqlite3 is a native addon and esbuild ships a platform binary —
# both need their install scripts to run. pnpm blocks build scripts by
# default, so they are allowlisted here for contributors and CI.
allowBuilds:
  better-sqlite3: true
  esbuild: true
